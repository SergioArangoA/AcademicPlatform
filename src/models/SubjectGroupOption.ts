/** Opción del select: un grupo del docente con su asignatura vinculada. */
export interface SubjectGroupOption {
    group_id: string;
    subject_id: string;
    label: string;
    groupName: string;
    subjectName: string;
    subjectCode: string;
}
